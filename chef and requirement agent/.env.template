# RequirementParser Agent 环境配置模板
# Requirements: 7.1, 7.2, 7.3, 7.4, 7.5
#
# 使用说明：
# 1. 复制此文件为 .env
# 2. 填写所有必需的配置项
# 3. 确保 .env 文件不被提交到版本控制系统

# ============================================
# Agent 基础配置
# ============================================

# Agent 名称
REQ_PARSER_AGENT_NAME=RequirementParserAgent

# 运行环境 (development, staging, production)
ENVIRONMENT=development

# ============================================
# DeepSeek API 配置 (Requirement 7.1)
# ============================================

# DeepSeek API Key (必需)
# 获取方式：访问 DeepSeek 官网申请 API Key
REQ_PARSER_DEEPSEEK_API_KEY=your_deepseek_api_key_here

# DeepSeek API 端点
REQ_PARSER_DEEPSEEK_API_ENDPOINT=https://www.sophnet.com/api/open-apis/v1/chat/completions

# DeepSeek 模型名称
REQ_PARSER_DEEPSEEK_MODEL_NAME=DeepSeek-V3.2

# ============================================
# 业务配置 (Requirement 7.2)
# ============================================

# 最大重试次数 (0-10)
REQ_PARSER_MAX_RETRIES=3

# 超时时间（秒，1-300）
REQ_PARSER_TIMEOUT_SECONDS=30

# 置信度阈值 (0.0-1.0)
# 低于此值将触发人工澄清流程
REQ_PARSER_CONFIDENCE_THRESHOLD=0.6

# ============================================
# 默认配置 (Requirement 7.3)
# ============================================

# 默认质量档位 (low, balanced, high)
REQ_PARSER_DEFAULT_QUALITY_TIER=balanced

# 默认宽高比 (16:9, 9:16, 1:1, 4:3, 3:4)
REQ_PARSER_DEFAULT_ASPECT_RATIO=9:16

# 默认分辨率
REQ_PARSER_DEFAULT_RESOLUTION=1080x1920

# 默认帧率
REQ_PARSER_DEFAULT_FPS=30

# ============================================
# 文件处理配置
# ============================================

# 最大文件大小（MB）
REQ_PARSER_MAX_FILE_SIZE_MB=100

# 支持的图片格式（逗号分隔）
REQ_PARSER_SUPPORTED_IMAGE_FORMATS=jpg,jpeg,png,webp

# 支持的视频格式（逗号分隔）
REQ_PARSER_SUPPORTED_VIDEO_FORMATS=mp4,avi,mov,mkv

# 支持的音频格式（逗号分隔）
REQ_PARSER_SUPPORTED_AUDIO_FORMATS=mp3,wav,aac,m4a

# ============================================
# 性能配置
# ============================================

# 批处理大小
REQ_PARSER_BATCH_SIZE=10

# 最大并发请求数
REQ_PARSER_MAX_CONCURRENT_REQUESTS=5

# ============================================
# 基础设施配置
# ============================================

# Event Bus URL (必需)
REQ_PARSER_EVENT_BUS_URL=redis://localhost:6379

# Blackboard URL (必需)
REQ_PARSER_BLACKBOARD_URL=http://localhost:8000

# ============================================
# 日志配置
# ============================================

# 日志级别 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# 日志格式 (json, text)
LOG_FORMAT=json

# ============================================
# 监控配置
# ============================================

# 是否启用指标收集
ENABLE_METRICS=true

# 指标导出端口
METRICS_PORT=9090

# ============================================
# 开发配置（仅开发环境）
# ============================================

# 是否启用调试模式
DEBUG=false

# 是否启用详细日志
VERBOSE=false
